{
  "name": "base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code 向け node + python のベース環境",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.9.0",
  "scripts": {
    "lint": "eslint src tests",
    "lint:fix": "eslint src tests --fix",
    "format": "prettier --write .",
    "test": "node --test",
    "deploy:cf": "npx --yes wrangler@latest pages deploy"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
